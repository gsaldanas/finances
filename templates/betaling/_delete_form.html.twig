<form method="post" action="{{ path('app_betaling_delete', {'id': betaling.id}) }}" onsubmit="return confirm('Weet je zeker dat je dit item wilt verwijderen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ betaling.id) }}">
    <button class="btn">Delete</button>
</form>
