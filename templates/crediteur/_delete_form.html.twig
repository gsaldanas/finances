<form method="post" action="{{ path('app_crediteur_delete', {'id': crediteur.id}) }}" onsubmit="return confirm('Weet je zeker dat je dit item wilt verwijderen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crediteur.id) }}">
    <button class="btn">Delete</button>
</form>
